{
  "name": "pec_1",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel serve index.html -p 8123",
    "build": "parcel build index.html --target web --no-source-maps --no-cache",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf dist .parcel-cache"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "posthtml-include": "^2.0.1"
  }
}
