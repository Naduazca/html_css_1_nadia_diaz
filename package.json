{
  "name": "pec_1",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "parcel:serve": "parcel index.html -p 8123",
    "parcel:build": "parcel build index.html --no-source-maps --no-cache",
    "clean": "rimraf dist .cache .cache-loader .parcel-cache",
    "dev": "npm-run-all clean parcel:serve",
    "build": "npm-run-all clean parcel:build"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "npm-run-all": "^4.1.5",
    "parcel": "^2.16.0",
    "posthtml-include": "^2.0.1",
    "rimraf": "^5.0.5",
    "sass": "^1.93.3"
  },
  "browserslist": [
    "> 0.5%",
    "last 2 versions",
    "not dead"
  ],
  "devDependencies": {
    "@parcel/transformer-sass": "^2.16.3",
    "sharp": "^0.34.5"
  }
}
