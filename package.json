{
  "name": "pec_1",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "parcel:serve": "parcel -p 8123 --target web",
    "parcel:build": "parcel build index.html --no-source-maps --no-cache",
    "clean": "rimraf dist .cache .cache-loader .parcel-cache",
    "dev": "npm-run-all clean parcel:serve",
    "build":"npm-run-all clean parcel:build",
    "stylelint": "stylelint src/assets/styles//*.scss",
    "stylelint:fix": "stylelint src/assets/styles//.scss --fix",
    "lint:css": "stylelint \"src/assets/styles/**/*.scss\" --allow-empty-input"
  },
  "dependencies": {
    "parcel": "^2.16.0",
    "date-fns": "^4.1.0",
    "sass": "^1.93.3",
    "@parcel/transformer-sass": "^2.16.0",
    "posthtml-include": "^2.0.1",
    "npm-run-all": "^4.1.5",
    "rimraf": "^5.0.5"
  },
  "devDependencies": {},
  "browserslist": [
    "> 0.5%",
    "last 2 versions",
    "not dead"
  ]
}